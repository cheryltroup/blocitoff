  <tr id="item-<%= item.id %>">
  	
  <td><%= item.name %>

  	<%= link_to "", user_item_path(current_user, item),
  	 method: :delete, class: 'glyphicon glyphicon-ok', remote: true %></td>

    <td><%= item.days_left %></td>
    <td><%= item.created_at.to_date %></td>
 
</tr>
